# Screen layout fixtures

Fixtures in this folder are used by `tests/ui/screen-layout-schema.test.ts`.

Run only the schema contract tests:

```bash
npx vitest run tests/ui/screen-layout-schema.test.ts --project unit
```

Run all tests (CI path):

```bash
npm test
```
