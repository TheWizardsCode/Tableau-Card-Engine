/**
 * Main Street Adapter – placeholder for migrating Main Street scene code
 * to use the shared Renderer API.
 *
 * Once the migration work-item (CG-0MPOLHCAN0037UUS) is completed, this
 * module will re-export or adapt existing helper names used by Main Street
 * scenes to call into `src/ui/Renderer`.
 *
 * @module MainStreetAdapter
 */

// TODO: migrate Main Street helpers into shared Renderer API.
// See work-item CG-0MPOLHCAN0037UUS for details.

export const MAIN_STREET_ADAPTER_VERSION = '0.1.0';

/**
 * Stub placeholder – will be replaced with actual adapter functions
 * that wrap the shared Renderer helpers with Main Street–specific defaults.
 */
export function initMainStreetAdapter(): void {
  // No-op stub until migration is complete.
}
