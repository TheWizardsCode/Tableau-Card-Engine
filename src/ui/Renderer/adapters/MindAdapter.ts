/**
 * The Mind Adapter – placeholder for migrating The Mind scene code
 * to use the shared Renderer API.
 *
 * Once the migration work-item (CG-0MPOLHCB400363VZ) is completed, this
 * module will re-export or adapt existing helper names used by The Mind
 * scenes to call into `src/ui/Renderer`.
 *
 * @module MindAdapter
 */

// TODO: migrate The Mind helpers into shared Renderer API.
// See work-item CG-0MPOLHCB400363VZ for details.

export const MIND_ADAPTER_VERSION = '0.1.0';

/**
 * Stub placeholder – will be replaced with actual adapter functions
 * that wrap the shared Renderer helpers with The Mind–specific defaults.
 */
export function initMindAdapter(): void {
  // No-op stub until migration is complete.
}
